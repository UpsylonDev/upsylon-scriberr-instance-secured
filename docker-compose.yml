services:
  scribberr:
    image: ghcr.io/rishikanthc/scriberr:latest
    container_name: scribberr
    restart: unless-stopped
    # 🔑 AJOUT CRITIQUE : Limite de mémoire pour Scribrr (2GB)
    # Ceci empêche le service de saturer votre RAM de 8GB.
    mem_limit: 2g
    # 🔑 LIEN VERS LE FICHIER ENV (Pour les variables ci-dessus)
    env_file:
      - .env
    # 🔑 AJOUT : Utilisation des variables d'environnement dans le conteneur
    environment:
    - WEB_URL=https://writer.upsylon.tech
    - BASE_URL=https://writer.upsylon.tech
    - APP_BASE_URL=https://writer.upsylon.tech # ⬅️ NOUVEL ESSAI
    # Ajouter d'autres variables si nécessaires par Scribrr

    volumes:
      - scriberr_data:/app/data
    networks:
      - mon_reseau_front

networks:
  mon_reseau_front:
    external: true

volumes:
  scriberr_data:
